# Copy this file to .env in this directory (neuropath-frontend/.env) and fill in real values.
# Vite exposes only VITE_-prefixed variables to client code (import.meta.env).

# Base URL of the backend API. Falls back to http://localhost:8000/api if unset.
VITE_API_URL=http://localhost:8000/api
